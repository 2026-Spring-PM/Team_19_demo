# assets/ — 게임 리소스 (단일 루트)

**`asset/`(단수) 폴더는 사용하지 않습니다.** 모든 파일은 이 `assets/` 아래에 둡니다.

| 하위 폴더 | 용도 (docs/32) |
|-----------|----------------|
| `fonts/` | UI·한글 폰트 |
| `tiles/` | 32×32 타일 스프라이트 (추가 예정) |
| `sprites/` | 캐릭터·오브젝트 (추가 예정) |

폰트 설치:

```bash
./scripts/setup_fonts.sh
```
